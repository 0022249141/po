# Validation Report — XAUUSD_o M5

## Dataset

- File: `XAUUSD_o_M5_202503210000_202609031255.csv`
- Source class: user-provided MT5 export
- Dataset ID: `xauusd_o_m5_202503210000_202609031255`
- SHA-256: `85432bdef0ce863df9522a93d473481b2869d6a8c7df96b2977c94c58c59cdd2`
- Rows: **103,037**
- First timestamp: `2025-03-21 00:00:00`
- Last timestamp: `2026-09-03 12:55:00`
- Timezone: **unresolved source-local**

## Observed schema

```text
<DATE> <TIME> <OPEN> <HIGH> <LOW> <CLOSE> <TICKVOL> <VOL> <SPREAD>
```

The file is tab-delimited despite the `.csv` extension. Dates use `YYYY.MM.DD`; time uses `HH:MM:SS`.

## Structural validation

| Check | Result |
|---|---:|
| Duplicate timestamps | 0 |
| Out-of-order timestamps | 0 |
| High/OHLC inconsistency | 0 |
| Low/OHLC inconsistency | 0 |
| Empty rows interpreted as records | 0 observed |
| Gaps > 7.5 minutes | 384 |

**Validation status:** `pass_with_warnings`.

The 384 gap warnings are not automatically classified as corrupt data. The dominant patterns are compatible with market/session closures, but they must remain explicit until a broker calendar/timezone is established.

## Gap profile

- 289 gaps of **1h05m** — dominant recurring source-session break pattern.
- 61 gaps of **2d01h10m** — dominant weekend pattern.
- 7 gaps of **10m** — each represents at least one missing expected M5 timestamp and requires case-specific review.
- Additional multi-hour/day gaps exist around holidays or possible feed interruptions.
- Maximum observed gap: **3d01h10m**.

No gap is forward-filled or synthesized by this project.

## Volume and spread

- `<TICKVOL>` is populated.
- `<VOL>` is zero for every row; it is **not** treated as exchange volume.
- `<SPREAD>` is present in broker/feed point units:
  - minimum: 4
  - median: 8
  - maximum: 301

A point-to-price conversion must use the exact symbol specification before these values are used as transaction costs.

## Closed vs forming bar

The last row begins at `2026-09-03 12:55:00`, and the filename also terminates at `202609031255`. The final row has tick volume 80, materially below the dataset median activity, but tick volume alone cannot prove that it was forming.

**Operational rule:** exclude the final 12:55 bar from any close-confirmed structure, BOS/CHoCH, candle-close signal, or backtest decision unless the exact export cutoff proves the bar had completed (source-local cutoff >= 13:00 for M5).

## Evidence boundary

1. The suffix `_o` is preserved as part of the symbol; broker/feed identity is not inferred from it.
2. Row timestamps are kept source-local until timezone is explicitly established.
3. This dataset can be used for price-based M5 research after the final-bar rule is applied.
4. It is not yet approved for timezone-sensitive session research, DST logic, or precise spread-cost conversion.
5. It must not be merged with Dukascopy, TradingView, OANDA or another provider without an explicit normalization and cross-feed audit.

## Next gate

To promote this dataset from `validated-price-data` to `fully-qualified-backtest-data`, recover:

- exact broker/feed identity;
- source/server timezone and DST policy;
- symbol digits/point size/contract specification;
- export cutoff including seconds;
- expected daily session calendar;
- decision on exclusion/classification of the 7 ten-minute gaps and other irregular closures.
