# XAUUSD_o M5 — Source-Day TPO Operational Smoke Test

**Status date:** 2026-09-04  
**Purpose:** verify the first operational `amt_tpo_profile_core_v1` implementation against a real user MT5-derived XAUUSD M5 export without inventing timezone or named-session semantics.

## Input provenance

- file: `XAUUSD_o_M5_202603230005_202609030920.csv`
- source role: user-provided MT5-derived OHLC export
- file size: `2,017,388` bytes
- SHA-256: `9503650ad91aa96aaf6cf921f48f457c55b974bd49ef9760412a9b77a1730452`
- rows in file: `32,242`
- source timestamp semantics: unresolved source-local time
- timeframe: `M5`
- explicit cutoff: `2026-09-03 09:22:00.092` source-local, inherited from the already-qualified H1/M15/M5/Tick bundle
- operational close rule: `bar_start + 5 minutes <= cutoff`

The raw CSV is not committed to the repository. Only provenance and reproducible results are stored here.

## Session policy used

Policy: `config/session-policies/source-calendar-day.yaml`

For this smoke test, `session_id = source-day:YYYY-MM-DD` is only a deterministic technical grouping based on the date encoded in the source timestamp. It is **not** interpreted as Asia, London, New York, exchange, broker-business-day, or canonical Market Profile session.

No timezone or DST offset was inferred.

## Price increment

`0.10` was supplied explicitly as a **research/profile bin width** for this smoke test.

It is not asserted to be the exchange tick size, broker point size, or a canonical Dalton/CME profile increment. The project still requires provenance-tracked symbol contract metadata before such a claim can be made.

## Current source-day result at cutoff

Target technical grouping: `source-day:2026-09-03`

Observed bars at or before cutoff for that date:

- first observed M5 bar: `2026-09-03 01:00:00`
- last closed M5 bar: `2026-09-03 09:15:00`
- M5 `09:20:00` bar: forming at the cutoff and excluded from occupancy
- closed bars contributing to current source-day profile: `100`
- intra-observed-segment M5 gaps: `0`
- profile completeness flag: `complete` for the observed `01:00–09:15` segment only

The absence of bars before `01:00` is not classified as an error by the neutral source-day policy because the actual broker/session calendar remains unresolved. Therefore this report does **not** claim a complete 24-hour trading session.

## Deterministic occupancy diagnostics

With research increment `0.10`:

- occupied integer price bins: `593`
- observed normalized low: `4381.00`
- observed normalized high: `4440.20`
- maximum occupancy count in any bin: `21`
- four bins share that maximum occupancy count

The maximum-count diagnostic is **not labelled POC**. POC selection is explicitly outside `amt_tpo_profile_core_v1` until a canonical project rule is source-frozen.

## Forming-bar control

The `2026-09-03 09:20:00` bar is present in the input file, but at cutoff `09:22:00.092` it has not completed its five-minute interval. It contributes zero TPO units.

This is consistent with the project's closed-vs-forming rule and prevents future information from entering the descriptive profile.

## Interpretation boundary

This test establishes only that:

1. the operational TPO engine can consume real XAUUSD M5 data deterministically;
2. an explicit cutoff correctly excludes the forming bar;
3. source-calendar-day grouping can be used as a neutral technical adapter while timezone semantics remain unresolved;
4. the resulting profile is descriptive time-at-price occupancy only.

It does **not** establish:

- London / New York / Asia session behavior;
- canonical Market Profile session construction;
- POC or Value Area;
- acceptance/rejection trading thresholds;
- initiative/responsive activity;
- an entry or exit signal;
- profitability or backtest evidence.

## Verdict

`PASS — OPERATIONAL ENGINE REAL-DATA SMOKE TEST`

The implementation is suitable for deterministic descriptive research on explicitly grouped source-local data. Named-session or strategy use remains blocked until timezone/session policy and downstream Strategy Specification are frozen.
