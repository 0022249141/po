# Multi-Timeframe Qualification — XAUUSD_o

## Bundle

Validated files:

- `XAUUSD_o_H1_202603230000_202609030900.csv`
- `XAUUSD_o_M15_202603230000_202609030915.csv`
- `XAUUSD_o_M5_202603230005_202609030920.csv`
- `XAUUSD_o_202609021900_202609030922.csv` (tick)

Operational bundle cutoff is anchored by the last tick: **2026-09-03 09:22:00.092 source-local**.

Timezone and broker/feed identity remain unresolved. The files are treated as one submitted export bundle because their symbol, timestamp alignment, naming and submission context are consistent; terminal-side export metadata has not independently proven that provenance.

## Individual structural quality

| Dataset | Rows | Duplicate TS | Out of order | OHLC/quote errors | Gap warnings | Status |
|---|---:|---:|---:|---:|---:|---|
| H1 | 2,691 | 0 | 0 | 0 | 117 | pass_with_warnings |
| M15 | 10,757 | 0 | 0 | 0 | 117 | pass_with_warnings |
| M5 | 32,242 | 0 | 0 | 0 | 117 + coverage-edge note | pass_with_warnings |
| Tick | 156,749 | 0 | 0 | 0 crossed quotes | 1 intertick gap >1m | pass_with_warnings |

`<VOL>` is zero on all H1/M15/M5 rows. `<TICKVOL>` is populated. Tick `<LAST>` and `<VOLUME>` are empty. None of these files are treated as centralized exchange volume/order-flow evidence.

## Gap alignment

The three bar files show the same **117 break events**, with timeframe-dependent interval lengths caused by bar spacing.

Dominant patterns:

- H1: 93 × `2h00m`, 19 × `2d02h00m`
- M15: 93 × `1h15m`, 19 × `2d01h15m`
- M5: 93 × `1h05m`, 19 × `2d01h10m`

Other longer gaps are preserved explicitly. No missing bar is forward-filled or synthesized.

The tick file contains one intertick break >1 minute: `2026-09-02 23:58:57.459` → `2026-09-03 01:01:00.102` (`1h02m02.643s`).

## Cross-timeframe OHLC reconstruction

Only complete child-bar groups were compared.

### M5 → M15

- complete comparable M15 groups: **10,730**
- exact OHLC matches: **10,730 / 10,730**
- exact summed tick-volume matches: **10,730 / 10,730**
- OHLC mismatches: **0**

### M5 → H1

- complete comparable H1 groups: **2,664**
- exact OHLC matches: **2,664 / 2,664**
- exact summed tick-volume matches: **2,664 / 2,664**
- OHLC mismatches: **0**

### M15 → H1

- complete comparable H1 groups: **2,688**
- exact OHLC matches: **2,688 / 2,688**
- exact summed tick-volume matches: **2,688 / 2,688**
- OHLC mismatches: **0**

This establishes exact internal price aggregation consistency for every complete parent interval compared.

## Tick → bar reconstruction

Tick BID quotes were bucketed by source-local bar boundaries. Only bars fully completed before the tick cutoff were compared.

| Reconstruction | Comparable completed bars | Exact OHLC | Mismatch |
|---|---:|---:|---:|
| Tick BID → M5 | 160 | 160 | 0 |
| Tick BID → M15 | 53 | 53 | 0 |
| Tick BID → H1 | 13 | 13 | 0 |

Therefore the observed bar OHLC is exactly reproducible from BID ticks throughout the common completed overlap.

Tick record count also equals bar `<TICKVOL>` for all but one M5 interval. At `2026-09-03 07:10`, M5 `<TICKVOL>` is **917** while the exported tick file contains **915** records. The same two-record difference rolls into the 07:00 M15 and H1 bars. OHLC remains exact. This discrepancy is retained as an explicit warning; its cause is not inferred.

## Closed vs forming bars at cutoff

With cutoff `09:22:00.092`:

- H1 `09:00` bar is **forming**; latest closed H1 = `08:00`.
- M15 `09:15` bar is **forming**; latest closed M15 = `09:00`.
- M5 `09:20` bar is **forming**; latest closed M5 = `09:15`.

Any BOS, CHoCH, MSS, close-confirmed break, candle-close entry or backtest decision must exclude these forming rows.

## Coverage edge

H1 and M15 begin at `2026-03-23 00:00`, while this M5 file begins at `00:05`. Therefore the first higher-timeframe interval cannot be fully reconstructed from M5 and is excluded from aggregation tests. No synthetic `00:00` M5 bar is created.

## Qualification verdict

**Status: MULTI-TIMEFRAME-QUALIFIED PRICE DATA — WITH TIMEBASE / SOURCE WARNINGS**

Approved uses:

- price-structure research;
- cross-timeframe OHLC analysis;
- closed-bar rule testing with the stated cutoff policy;
- BID-based reconstruction over the tick overlap;
- strategy research that does not depend on unresolved session timezone or centralized volume.

Not yet approved for:

- London/NY/Asia session rules or DST-sensitive research;
- fully qualified transaction-cost backtesting based on symbol point/contract metadata;
- real exchange order flow, CVD, DOM or dealer-inventory claims;
- provider-spliced backtests without normalization;
- treating the remaining tick-count discrepancy as resolved.

## Promotion requirements

To promote this bundle to `fully-qualified-backtest-data`, record exact broker/feed identity, source/server timezone and DST policy, symbol digits/point/contract/tick-value metadata, daily trading-session calendar, and a documented explanation or acceptance policy for the two-record tick-volume discrepancy.
